adb --version

emulator -version

sdkmanager --licenses

emulator -list-avds

emulator -no-snapshot -avd <emulator>

react-native init --version="0.56.0" nomeProjeto

react-native run-android

Redux => state container

props => somente leitura

state => manipular dados